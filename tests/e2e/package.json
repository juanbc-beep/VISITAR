{
  "name": "visitar-e2e",
  "private": true,
  "type": "module",
  "description": "Suite de regresión de interfaz (Playwright + simulador de Supabase). Ver LEEME.md.",
  "scripts": {
    "test": "bash correr.sh"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
